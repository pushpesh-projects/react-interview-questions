
Images -
portrait - images are taller than wider
1. why do we need to wrap images in a parent container.
   e.g. in one of the scenario, we created a parent container and give it width and height of the image and overflow hidden and then we define css properties for image where we gave it height and width 100% and object-fit : cover.

2. what is collapasing margin in css?
3. flex properties -
   { display: flex
   justify-content: center
   align-items: center
   flex-shrink: 0
   }
4. box-sizing: border-box;
5.   max-height: 400px;
    max-width: 400px;
6. .dice-container {
    display: grid;
    grid-template: auto auto / repeat(5, 1fr);
    gap: 20px;
}
7.  box-shadow: 0px 2px 2px rgba(0, 0, 0, 0.15);
8.  font css
   font-family: Inter, sans-serif;
    font-size: 1.75rem;
    font-weight: bold;

9. cursor: pointer;
10. border-radius: 10px;
    border: none;
11.
